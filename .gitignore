# Binaries (anchored to repo root so they don't shadow directories)
/torana-edge
/torana
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo

# WASM plugin binaries (built via torana-cli or GOOS=wasip1)
*.wasm

# Environment (legacy — config is now JSON-based)
.env

# OS
.DS_Store
Thumbs.db
config.json

# Local runtime artifacts (dogfood binaries, logs) and MITM CA material.
# The MITM CA private key MUST never be committed or trusted system-wide.
/local/
*.pem
ca-key.pem
bundle.pem
