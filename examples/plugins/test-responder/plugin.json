{
  "schema_version": 1,
  "id": "torana-test/test-responder",
  "name": "test-responder",
  "version": "0.1.0",
  "abi_version": "v2",
  "failure_mode": "pass",
  "description": "Example plugin: serves a canned completion via env.respond_request when a message contains 'respondme'",
  "hooks": [
    {
      "name": "run_before_request"
    }
  ],
  "permissions": [
    {
      "name": "env.respond_request",
      "description": "Serve the response directly without calling upstream"
    },
    {
      "name": "env.log",
      "description": "Diagnostic logging"
    }
  ]
}
