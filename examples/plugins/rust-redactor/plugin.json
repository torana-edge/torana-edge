{
  "schema_version": 1,
  "id": "torana-test/rust-redactor",
  "name": "rust-redactor",
  "version": "0.3.0",
  "abi_version": "v1",
  "failure_mode": "pass",
  "description": "ABI v1 smoke fixture (Rust). NOT loadable by this host, which is v2-only: it still exports run_before_request(request_id, ptr, len) and decodes torana.v1. Excluded from TESTDATA_DIRS and never built. Port it when the Rust SDK moves to ABI-core v2.",
  "hooks": [
    {
      "name": "run_before_request"
    }
  ],
  "permissions": []
}
