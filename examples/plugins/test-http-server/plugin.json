{
  "schema_version": 1,
  "name": "test-http-server",
  "version": "0.1.0",
  "description": "Test fixture: serves an HTML page and a schema-validated agent operation over run_on_http_request",
  "hooks": [
    {"name": "run_on_http_request", "priority": 100}
  ],
  "permissions": [
    {"name": "env.serve_http", "description": "Serve pages and JSON under /_torana/plugin/test-http-server/"},
    {"name": "env.log", "description": "Diagnostic logging"}
  ]
}
