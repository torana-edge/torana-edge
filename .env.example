# Torana Edge Configuration
# Copy this file to .env and fill in your provider details.

# Which upstream provider to proxy to: "anthropic" or "openai"
UPSTREAM_PROVIDER=anthropic

# Base URL of the upstream API (no trailing slash)
UPSTREAM_URL=https://api.anthropic.com
