{
  "port": 8080,
  "providers": {
    "deepseek": {
      "url": "https://api.deepseek.com",
      "format": "openai"
    },
    "deepseek-anthropic": {
      "url": "https://api.deepseek.com/anthropic",
      "format": "anthropic"
    }
  },
  "plugins": {
    "dir": "./plugins",
    "_comment": "Safe coding-agent baseline: intent instrumentation stays enabled, but lossy compactors are disabled until issue #166 protects fresh/exact file reads. For research-only sessions, append ONE compactor after intent: keyword_compactor or compactor, never both.",
    "order": ["schema_translator", "intent"]
  },
  "offload": {
    "enabled": false,
    "provider": "deepseek",
    "model": "deepseek-v4-flash"
  },
  "limits": {
    "concurrency": 10,
    "rpm": 100
  }
}
